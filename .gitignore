# OS / IDE
.DS_Store
.idea/
.vscode/
*.iml

# Build / deps
target/
node_modules/
dist/
.angular/

# Logs
*.log
npm-debug.log*

# Secrets (compose env, JWT keys)
infra/.env
backend/src/main/resources/privateKey.pem
backend/src/main/resources/publicKey.pem
backend/.env

# Local Quarkus
.quarkus/

# User-uploaded files (action / proposal cover images). FileService writes
# them to backend/uploads/ at runtime; we never commit them.
backend/uploads/

# claude-preview tooling
.claude/
